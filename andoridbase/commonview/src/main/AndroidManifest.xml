<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.me.diankun.commonview">


    <!-- 访问网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RadioCheckActivity" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".LayoutActivity" />
        <!--listview相关的Activity -->
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ListViewChatActivity" />
        <activity android:name=".ListViewEditActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"/>
        <activity android:name=".ListViewFlexibleActivity" />
        <activity android:name=".ListViewNotifyActivity" />
        <activity android:name=".ListViewGetViewActivity" />
        <activity android:name=".ListViewScrollHideActivity" />


        <!-- Service -->
        <service android:name=".service.UpdateTextService" />
    </application>

</manifest>
